
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css">
</head>
<body>
@include('partials/navbar')
<section class="mt-4">
  <div class="container">
    <h2>We are Bangladeshi</h2>
    <div class="row g-4">
      
        @each(user in users)
        <div class="col-xl-4 col-lg-4 col-md-4 col-sm-12 col-12">
        <div class="card" style="width: 18rem;">
         <img src="{{user.image}}" class="card-img-top" alt="{{user.name}}">
         <div class="card-body">
           <h5 class="card-title">{{user.name}}</h5>
           <a href="{{route('news_search',[user.id])}}">
            <p class="card-text">{{user.content}}</p>
           </a>
           <div class="d-flex justify-content-between align-items-center">
            <div class="btn-group">
              <a href="{{route('news_edit',[user.id])}}" class="btn btn-sm btn-outline-secondary">Edit</a>

              <form action="{{route('news_delete',[user.id])}}?_method=DELETE" method="POST">
                {{ csrfField() }}
                <button class="btn btn-sm btn-outline-secondary">Delete</button>
              </form>
            </div>
           </div>
         </div>
       </div>
      </div>
        @endeach
      
    </div>
  </div>
</section>
@include('partials/footer')
</body>
</html>